# Community redesign v1 (Conservative hub)

Archived when the **Evolutionary** hub (`redesign_v2/HubEvolutionary`) replaced `HubConservative` on `/community`.

Shared utilities (`RichContent`, `MiniChart`, `Avatar`, `SkillBadge`) remain here and are imported by v2 components.

To roll back the page entry point, change `src/app/(dashboard)/community/page.js` to import `HubConservative` from this folder.
