/**
 * Re-export of the shared shadcn-style `Card` with mobile-first padding/radius
 * (see `src/components/ui/card.jsx`). Import from here in feature code when
 * you want to signal “page shell / layout” vs low-level UI primitives.
 */
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from '@/components/ui/card';
