# Legacy Learning Center components

Pre-redesign components for `/learning-center`. Kept for rollback safety.

Rollback path:

1. Restore `src/components/learning/LearningCenterPage.jsx` from this folder
2. Restore the previous `learning-center.css` from git history
3. Commit and push

Files:

- `LearningCenterPage.jsx` — old main hub
- `FriendsLearningCard.jsx` — old friends activity card
- `LearningCenterHero.jsx` — old hero banner
- `CoursePreviewSection.jsx` — old course preview cards
